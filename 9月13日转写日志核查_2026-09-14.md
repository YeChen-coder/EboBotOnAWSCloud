# 9 月 13 日转写日志核查

核查日期：2026-09-14。范围：多伦多时间 2026-09-13 00:00–24:00（EDT），对应 UTC 2026-09-13 04:00 至 2026-09-14 04:00。只读核查，未重启服务、改动配置或触发模型测试。

## 核查结论

当天有 **5 条 `conversation.user.transcript` 记录，但全部正文为空**。每条均为 `transcript: ""`、`transcript_chars: 0`、`persisted: true`。没有非空转写，也没有新增 `conversation.assistant.output`。

用户打开的旧地址限定日志流 `realtime-assistant/realtime-assistant/<task-id-redacted>`。该 Task 在 9 月 12 日降内存时停止，之后的日志转入新 Task 的日志流：

`realtime-assistant/realtime-assistant/<task-id-redacted>`

因此，“旧页面没有 9 月 13 日记录”与“当天没有有效转写正文”是两个同时存在的情况。前者是查看范围问题；后者是应用确实收到了空转写。

| 多伦多时间，9 月 13 日 | CloudWatch UTC 时间 | 正文字数 | 持久化 |
|---|---|---:|---|
| 01:45:23 | 05:45:23 | 0 | true |
| 01:48:58 | 05:48:58 | 0 | true |
| 09:01:19 | 13:01:19 | 0 | true |
| 17:11:42 | 21:11:42 | 0 | true |
| 17:21:43 | 21:21:43 | 0 | true |

## 为什么没有生成回复

逐条对照应用日志，每次先出现 `user transcript received (chars=0)`，随后写入结构化转写记录，再出现忽略空或低信息量音频回合的日志。应用源码 `_handle_input_transcript` 在收到 `conversation.item.input_audio_transcription.completed` 后，先持久化转写，再判断是否有可响应的内容；本次五条均在空转写分支被忽略。

这说明五次最终转写事件已经到达应用，且在 CloudWatch 格式化之前正文就为空。不是原本有文字却在日志里丢掉，也不是完全没有触发任何转写事件。日志中的 `persisted: true` 表示应用成功完成 JSONL 写入；本次未额外挂载 EFS 逐条重读文件。

当天另有 4 条 `realtime.response.done`。通过前后的应用日志核对，它们均对应会话轮换前生成 handoff memory 的内部请求，不是对用户播放的对话回复。`assistant_outputs_persisted` 全天保持 10，不能把这 10 条历史存量当成当天新回复。

## 当天服务运行情况

- Assistant 共 17,108 条日志，全部来自新 Task 日志流；其中健康快照 5,759 条。健康快照最大间隔约 15.08 秒，覆盖整天，没有发现长时间日志中断。
- Engine 也有 5,759 条健康快照。两个应用各自 boot_id 全天不变，Assistant uptime 从 27,152.8 秒持续增加至 113,535.7 秒，没有应用重启迹象。
- Assistant 5,759 个健康采样中，5,754 个 `ok=true`。3 个采样源音频状态异常，2 个采样处于 Realtime 切换；音视频传输多数时间正常。这些状态只能证明数据流存在，不能证明其中有清晰可识别的人声。
- 26 次 Realtime 断开都标记 `planned=true`，连接事件也是 26 次；计划轮换计数从 8 增至 34，非计划重连计数保持 0。
- 多伦多时间约 03:42、04:36、22:47 出现 3 条短暂 `no_source_packets` 警告。随后健康恢复，不能据此解释全天缺少非空转写，也没有证据将这 5 次空转写归因于这些断流。
- 当天未发现 `conversation.transcription.failed`、`realtime.server_error`、文件持久化失败或 ERROR 级日志。

现有证据不支持“降到 2 GiB 导致服务整天停止或日志停止”的解释。但不能仅凭空转写断定当天没人说话：背景声、收音音量、声音清晰度以及语音检测/识别行为，都需要结合实际说话时段和音频证据进一步区分。现有日志没有足够的人声质量或逐次 VAD 诊断数据，无法追溯确定空正文的声学原因。

## 今后查看方式

进入 CloudWatch → 日志组 `/ebo-cloud/assistant` → **搜索所有日志流**，避免固定到某个 Task ID。过滤：

```text
{ ($.event = "conversation.user.transcript" || $.event = "conversation.assistant.output") && $.service = "realtime-assistant" }
```

UTC 时区下查询 9 月 13 日 04:00 至 9 月 14 日 04:00，等价于多伦多 9 月 13 日全天。控制台已切到这个范围，并实际核对上述五条记录。`transcript` 字段为空就是本次没有识别出文字。

原始证据保存在本地 `.local/transcript-audit-20260913/`：完整当日 Assistant 日志、Engine 健康快照、ECS 服务状态与汇总。源码参考 `realtime-assistant/app.py` 的 `TranscriptStore.append`、`_handle_input_transcript` 及空文本判断分支。
