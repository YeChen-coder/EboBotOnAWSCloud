# Fargate 内存降配评估

评估对象：加拿大中部 `ca-central-1` 的 `ebo-cloud-lab` 正式服务；一个 Fargate Task，内含 `ebo-engine` 和 `realtime-assistant`。金额为美元 USD，未税、不含外部 API。

## 结论与实测依据

已完成从 **1 vCPU / 4 GiB 降至 1 vCPU / 2 GiB**，并通过部署及业务健康验收。按历史已观测峰值 320 MiB 计算，2,048 MiB 容量仍余 **1,728 MiB（84.38%）**，容量约为观测峰值的 6.4 倍。用户已授权在余量充足时直接降配，最终验收结果见文末。

查询覆盖 2026-09-10 00:00 UTC 至 2026-09-12 20:20 UTC。正式服务实际有数据的时间为 **9 月 10 日 03:20 至 9 月 12 日 20:19 UTC**，约 65 小时，共 3,899 个一分钟服务数据点。查询按 `ClusterName + ServiceName` 限定正式服务，排除独立音频探针和日志合成测试 Task。未将无数据分钟当成零使用量。

| 指标 | 观测结果 | 解释 |
|---|---:|---|
| Task 平均内存 | 303.05 MiB | Container Insights 的分钟平均值再取平均 |
| Task 内存最高值 | 320 MiB，原配额的 7.8125% | AWS/ECS `MemoryUtilization` 的一分钟 `Maximum`；9 月 11 日 17:02 UTC |
| Task 内存交叉核对 | 319 MiB | Container Insights `MemoryUtilized` 最大采样，包含 EFS 管理进程；不同采样口径有约 1 MiB 差异 |
| Engine 单独内存峰值 | 166 MiB | `ContainerMemoryUtilized` |
| Assistant 单独内存峰值 | 103 MiB | `ContainerMemoryUtilized` |
| EFS supervisor 内存峰值 | 53 MiB | AWS 自动管理进程，也消耗 Task 内存 |
| CPU 使用率峰值 | 58.09% | 9 月 10 日 12:47 UTC，已使用 1 vCPU 规格；约 0.581 vCPU |
| 1 vCPU 阶段平均 CPU | 39.56% | 9 月 10 日 07:09 UTC 后的 3,670 个数据点；未混入原 2 vCPU 阶段 |

各容器峰值时间不同，不能相加后当成同时发生的 Task 峰值。Container Insights 内存的单位标签为 `Megabytes`，这里按 ECS 的 MiB 口径解读，并核对到预留指标恒为 4096，避免仅以容器软预留百分比推算 Task 容量。

降配前正式 Task 从 9 月 11 日 17:06 UTC 运行至本次检查，约 27 小时。当前 ECS 两容器 HEALTHY；Assistant uptime 98,290.6 秒、音视频输入和 Realtime 均正常，健康快照记录 20 次转写事件、10 条已持久化回复、27 次计划会话轮换、3 次非计划 Realtime 重连，最近一次播放 17.2 秒。回复持久化计数可能包括从 EFS 恢复的历史记录，不能全部视为这段时间新生成的回复。Realtime 重连不是容器重启。ECS 当前部署 failedTasks=0，近期可查询的 STOPPED Task 列表为空；这并不是覆盖全部历史的 OOM 审计。

这些是已发生负载的采样峰值，**不是瞬时绝对峰值，也不是压力测试得到的最大承载能力**。AWS 的服务指标由周期采样汇总，可能漏掉更短的尖峰。已有样本支持保留充足余量后降配，但不能保证未来任意并发、超长回复或新功能都无影响。参见 [AWS ECS 指标采样与统计说明](https://docs.aws.amazon.com/AmazonECS/latest/developerguide/service_utilization.html) 和 [Container Insights 指标定义](https://docs.aws.amazon.com/AmazonCloudWatch/latest/monitoring/Container-Insights-enhanced-observability-metrics-ECS.html)。

## 配置与费用

| 项目 | 降配前 | 降配后 |
|---|---:|---:|
| Task CPU | 1024（1 vCPU） | 1024（1 vCPU） |
| Task 内存 | 4096 MiB | 2048 MiB |
| Engine 内存软预留 | 2304 MiB | 1024 MiB |
| Assistant 内存软预留 | 1536 MiB | 768 MiB |
| CPU 月费，730 小时 | $32.53 | $32.53 |
| 内存月费，730 小时 | $14.21 | $7.10 |
| Fargate CPU + 内存合计 | **$46.73** | **$39.63** |

两个业务容器软预留合计 1792 MiB，另留 256 MiB 容量空间；软预留不是新增单容器硬上限，容器可在 Task 总容量内使用可用内存。仍需给 EFS 管理进程及日志缓冲留空间。AWS 支持 1 vCPU 配置 2 GiB 内存，见 [Fargate Task 配置表](https://docs.aws.amazon.com/AmazonECS/latest/developerguide/task_definition_parameters.html)。

2026-09-12 重新读取的加拿大中部 Linux x86_64 官方按量单价：CPU $0.04456/vCPU小时，内存 $0.004865/GiB小时。每完整 730 小时运行月节省 **$7.1029**，Fargate 算力费用下降约 **15.2%**。按 12 个同样的计费月约省 $85.23。来源：[AWS 区域价格目录](https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AmazonECS/current/ca-central-1/index.json)、[Fargate 计费规则](https://aws.amazon.com/fargate/pricing/)。变更前已运行时段仍按 4 GiB 计费。

如果沿用 9 月 10 日成本报告的网络、日志、存储用量假设，AWS 整体由约 $77–88/月降至约 **$70–81/月**。这是旧基线减去内存差价的外推，**不是本次重新测量所有资源后的账单预测**；近期新增对话正文日志、网络和回复文件增长会影响实际金额。Container Insights Enhanced、日志保留期、EFS、备份、公网 IPv4 等项目保持原配置。

## 变更与验收

CloudFormation 变更集：`memory-2gib-20260912`。已核对本地生成模板与线上模板，差异仅为 Task 总内存和两个容器的软预留；变更集仅涉及 TaskDefinition 与 Service。镜像 digest、CPU、Secrets、EFS、网络与观测配置沿用线上值。单 Task 部署策略会先停旧 Task，再启动新 Task，因此会有短暂连接中断。

本地已保留原模板及参数快照在 `.local/memory-review/`，可通过 CloudFormation 恢复 4096 MiB 和原软预留，无需重新构建镜像。

**最终验收：2026-09-12 20:31 UTC（多伦多 16:31）通过。** CloudFormation `UPDATE_COMPLETE`，ECS 单一部署 `COMPLETED`，desired/running=1、pending=0；任务定义 `ebo-cloud-lab:6`，新 Task `<task-id-redacted>` 实际 CPU=1024、Memory=2048，两容器 HEALTHY。旧 Task 已因本次服务调度正常 STOPPED。

Assistant 运行 207.9 秒，超过 120 秒启动宽限期，业务 ok=true；Engine 已发现一个机器人并收到音频，Assistant 音视频输入、源音频及 Realtime 连接均正常。最初三个新 Task 分钟指标的内存最高 **277 MiB（13.53%）**，预留恒为 2048 MiB；CPU 分钟采样最高约 434 单位（0.424 vCPU）。核对新旧任务定义时按容器名称对齐，确认实际配置差异仅为预期内存变更。没有重新构建镜像，也未修改应用代码。

这次完成了短时运行验证，没有主动触发额外模型调用或真人听说测试。未来高强度对话的播放质量、延迟和峰值仍须由实际负载确认；未创建后台自动监控任务。

证据：`.local/memory-review/` 中保存查询窗口、3,899 点原始服务指标、容器级明细、降配前配置、健康快照、官方区域单价和变更集，均为本地工作材料，不含 Secrets 正文。
