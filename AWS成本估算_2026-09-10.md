# EBO Fargate 的 AWS 成本估算

调查日期：2026-09-10。范围：本次部署在账户 `<aws-account-id>`、加拿大中部 `ca-central-1` 的 EBO 云环境。**全部金额为美元 USD，未含税；不包含 OpenAI、Enabot 等外部 API 费用。**

2026-09-12 内存降配更新：已观测约 65 小时的 Task 内存峰值约 320 MiB，已按用户授权切换到 1 vCPU / 2 GiB 并通过部署与业务健康验收。该规格 Fargate CPU + 内存为 **$39.63/月**，比下文 4 GiB 基线每月省 **$7.10**。沿用原用量假设，AWS 整体约 **$70–81/月**；本次未重新盘点所有资源的用量，9 月 11 日增加正文日志后的日志量也未套用原 $0.20 作为新实测。最新部署结论见 [内存降配评估](Fargate内存降配评估_2026-09-12.md)。下文保留 9 月 10 日调查和当时配置，便于追溯。

## 结论（9 月 10 日基线）

按 **1 vCPU / 4 GiB** 降配后的配置，并假定日志与网络速度保持本次实测水平，连续运行 **730 小时/月**，预计 **约 $77–88/月**。原先 2 vCPU / 4 GiB 的估算约 $110–120/月，CPU 降配每完整运行月节省 $32.53。降配部署状态见文末补充。可先留 $90–105/月预算余量；这不是费用上限，也未替你创建预算或告警。

当前主要开销为 Fargate 预留算力，其次是 Container Insights 指标和网络出站。**两路应用日志条数看起来多，但按当前字节量外推，写入费约 $0.20/月。**

下面的估算使用实时 AWS 资源盘点、CloudWatch 数据以及 AWS 官方加拿大中部价格目录；不是已出账金额。不假定新账户赠金、促销抵扣、Savings Plans、Spot 折扣或付费支持计划。网络的账户共享免费额度分别计算，CloudWatch 免费额度只作为潜在减免说明。

## 1. 降配前实测范围与速度

测量窗口为 **2026-09-09 23:48 至 2026-09-10 02:48，多伦多时间**，共 3 小时，避开初次部署的大部分启动活动。应用尚未完成用户人工听说测试，因此这是当前保持连接状态的样本，不能代表密集对话或故障时的最高用量。

| 项目 | 实测结果 |
|---|---:|
| Fargate | 1 个 Task，Linux/x86_64，2 vCPU、4 GiB |
| 业务容器 | Engine、Realtime Assistant 共 2 个 |
| CPU 平均使用率 | 12.51%；最高一分钟平均 13.54% |
| 内存平均使用率 | 7.20% |
| Task 发送速率 | 平均 84,951 字节/秒，约 85 KB/s |
| Task 接收速率 | 平均 108,413 字节/秒，约 108 KB/s |
| Container Insights | 列出 235 条指标时间序列，其中最近一小时实际持续上报 212 条；23 条为非活跃序列 |
| 应用自定义指标 | 4 条：ApiReachable、BusinessReady、RealtimeConnected、SourceAudioOk |
| EFS 当前计量大小 | 79,872 字节，约 78 KiB；尚无已完成备份恢复点 |
| 镜像 | 两个发布版本，压缩镜像合计约 572 MB，未重复计算清单索引指向的相同镜像 |

CloudWatch 指标里还会出现 AWS 管理的 `aws-fargate-supervisor`，用于 EFS。它不是另开一个付费 2 vCPU Task；其算力消耗包含在当前 Task 配额内，但相关性能时间序列会影响监控计量。

## 2. AWS 月费明细

下表按 1 vCPU / 4 GiB、其余资源和实测速度不变计算，采用按量价格。官方单价来源为 [Fargate 区域目录](https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AmazonECS/current/ca-central-1/index.json)、[CloudWatch 区域目录](https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AmazonCloudWatch/current/ca-central-1/index.json)、[VPC 区域目录](https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AmazonVPC/current/ca-central-1/index.json)。

| 费用项 | 数量与单价 | 估算 USD/月 |
|---|---|---:|
| Fargate CPU | 1 × 730 小时 × $0.04456/vCPU小时 | **32.53** |
| Fargate 内存 | 4 × 730 小时 × $0.004865/GiB小时 | **14.21** |
| 公网 IPv4 | 1 个 × 730 小时 × $0.005/小时 | **3.65** |
| Container Insights Enhanced | 212 条活跃时间序列 × $0.07/指标月 | **14.84** |
| 应用自定义指标 | 4 条 × $0.30/指标月，未扣免费额度 | **1.20** |
| 两路应用日志写入 | 约 0.355 GiB/月 × $0.55/GB | **0.20** |
| 应用和性能日志存储 | 应用保留 14 天，性能日志当前保留 1 天；按未压缩字节保守估算 | **不足 0.01** |
| ECR 私有镜像存储 | 约 0.53 GiB × $0.10/GB月，向上取整 | **约 0.06** |
| Secrets Manager | 2 个 Secret × $0.40/月 | **0.80** |
| EFS、备份、少量密钥及 Secret 请求 | 当前文件量很小；详见增长模型 | **当前规模约 0.01 以内** |
| **上述小计，不含互联网出站** | 未扣 CloudWatch 免费额度；分项四舍五入 | **约 67.50** |
| 网络出站：100 GB 共享免费额度可用 | 以 Task 发送速率近似估算 | **约 9.7–11.1** |
| 网络出站：免费额度已被其他资源用完 | 同样流量按完整阶梯单价估算 | **约 18.7–20.1** |
| **合计：免费出站额度可用** | | **约 77–79** |
| **合计：免费出站额度不可用** | | **约 86–88** |

ECR、Secret、EFS、备份价格分别来自 [ECR 目录](https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AmazonECR/current/ca-central-1/index.json)、[Secrets Manager 目录](https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AWSSecretsManager/current/ca-central-1/index.json)、[EFS 目录](https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AmazonEFS/current/ca-central-1/index.json)、[AWS Backup 目录](https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AWSBackup/current/ca-central-1/index.json)。

**CPU 只使用 12.5%，不会让 Fargate CPU 账单自动变成 12.5%。**Fargate 按请求的 Task 配额与运行时间计费；降配后的请求为 1 vCPU / 4 GiB。两个容器共享这份 Task 配额，没有分别再收一份的费用。默认临时存储额度没有额外扩容费用。[Fargate 计费规则](https://aws.amazon.com/fargate/pricing/)

## 3. 为什么日志条数多，却没有很贵

| CloudWatch 日志组 | 3 小时条数 | 3 小时原始写入字节 | 外推每天条数 | 外推每月数据量 | 月写入费用 |
|---|---:|---:|---:|---:|---:|
| `/ebo-cloud/engine` | 1,569 | 691,513 | 12,552 | 0.157 GiB | $0.086 |
| `/ebo-cloud/assistant` | 735 | 876,144 | 5,880 | 0.199 GiB | $0.109 |
| `/aws/ecs/containerinsights/ebo-cloud-lab/performance` | 1,080 | 1,621,495 | 8,640 | 0.367 GiB | 已包含在 Enhanced 指标费里，不重复计写入费 |

两路应用日志共约 **18,432 条/天**，但每条通常只是几百字节到一千多字节，因此月数据量仍不足 0.4 GiB。这里使用的是 CloudWatch `IncomingBytes`、`IncomingLogEvents` 实测值；日志组 `storedBytes` 当前显示 0，存在计量更新延迟，不能因此认定没有费用。

ECS 的 Container Insights Enhanced 按指标月收费，包含性能指标存储及对应性能日志的写入，性能日志存储另计。它与 EKS 的 observation 计价不同，也不能把这里的 212 条指标全部套用普通自定义指标的 $0.30。普通应用日志依然独立计费。[CloudWatch 定价与 ECS Enhanced 例子](https://aws.amazon.com/cloudwatch/pricing/)

应用日志当前保留 14 天，性能日志当前实际保留 1 天；以上来自实时盘点。日志存储按压缩后数据计费，表中的未压缩估算偏保守。在当前数据量下，把应用日志保留从 14 天缩到 7 天，只能省很少的钱。

若错误风暴让两路应用日志长期增加到现在的 **10 倍**，写入约 $1.95/月；**100 倍**约 $19.54/月。这里假设每条平均大小不变，且不扣免费额度。未来如果记录大段请求体、转录正文或图像 Base64，则不能再沿用目前每条日志的大小估计。

CloudWatch 标准免费额度可进一步抵扣部分日志和普通指标费用；这是账户共享额度，本表没有假定它全部留给本项目，也不把它抵扣到 Enhanced 指标费上。[CloudWatch 免费额度和收费项](https://aws.amazon.com/cloudwatch/pricing/)

## 4. 流量：不算外部 API 调用费，但要算 AWS 发出去的数据

Task 实测发送速率约 85 KB/s，按 730 小时推算约 **208 GiB（223 GB 十进制）/月**。按加拿大中部首档互联网出站 **$0.09/GB** 估算，得到上面的两种网络费用情景。范围覆盖单位展示差异，避免给字节到计费 GB 的换算制造虚假精度。[出站区域单价](https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AWSDataTransfer/current/ca-central-1/index.json)

这里采用 **“所有 Task 发送字节都近似当作互联网出站”** 的保守模型。Container Insights 网络指标不是账单上的 Internet DataTransfer-Out：里面可能包含同区域 AWS 服务及 EFS 访问。准确可收费字节最终以账单用量类型为准；本次未开启额外的 VPC Flow Logs 来增加费用和数据量。

AWS 每月有跨服务、跨区域共享的 100 GB 互联网出站免费额度，不能给本项目和其他项目各扣一次。入站流量通常免费；两个同 Task 容器通过 localhost 传 RTSP，不应把内部媒体流再次算成公网出站。[AWS 数据传输规则](https://aws.amazon.com/ec2/pricing/on-demand/)

由于应用会持续发送输入音频，即使没人说话，AWS 发出的数据也不一定降到零。这笔 AWS 网络费用与 OpenAI 是否按音频 token 收费是两件事；本报告没有计算后者。

## 5. 存储、备份与当前未发生的费用

EFS 使用 **Regional Standard + Bursting**：$0.33/GB月，没有配置额外的 Provisioned Throughput 或 Elastic Throughput。因此不能套用 Elastic Throughput 的每 GB 读写费。当前计量只有约 78 KiB，存储费用极小。[EFS 定价](https://aws.amazon.com/efs/pricing/)

应用将生成的回复 WAV 和转录保存在 EFS。代码使用 24 kHz、单声道、16-bit PCM，**每累计 1 小时实际回复音频约 0.161 GiB**。如果每天生成 1 小时回复且不清理，30 天约增加 4.83 GiB；均匀增长时，第一个月新增 EFS 平均容量约 2.41 GiB，即约 $0.80，第 30 天容量对应约 $1.59/月的后续存储速率。长期不清理会逐月增加，当前“几乎为零”的存储费不能永久外推。

自动备份每天运行，保留 35 天，使用普通 warm backup vault；加拿大中部 EFS warm backup 单价为 $0.055/GB月。备份实际保留的独立数据和变更决定用量，不能直接当成每天 35 份完整副本。本次盘点尚无完成的恢复点，所以只给单价和增长模型，不编造已使用备份容量。[备份定价](https://aws.amazon.com/backup/pricing/)

EFS 和 Backup 的加密密钥均核验为 AWS managed，没有客户自建 KMS key 的固定月费；请求计费如发生仍是另一项，但当前小量使用下不是成本主体。[KMS 定价](https://aws.amazon.com/kms/pricing/)

| 项目 | 本次状态与成本处理 |
|---|---|
| NAT Gateway、负载均衡器、VPC Endpoint | 未部署，无相应小时费或处理费 |
| ECS Cluster 控制层、IAM 角色、子网、安全组、路由、Internet Gateway | 无本方案独立固定月费；流量和公网 IP 已另算 |
| 跨 AZ EFS 访问 | Task 所在 AZ 有对应挂载目标，按正常同 AZ 挂载估算；未加跨 AZ流量预算 |
| ECR 镜像扫描 | 实际为 BASIC，未开启付费 Enhanced Inspector 扫描；同区域 ECR 拉取不另加互联网出站 |
| CloudWatch 自建付费 Dashboard、Alarm | 本次没有创建；控制台自动概览不等于另建付费 Dashboard |
| AWS DevOps Agent、ADOT/CloudWatch Agent 独立容器 | 没有开通或部署，未计费用 |
| Lambda、CodeBuild、客户自建 S3 Bucket | 本次没有创建；镜像是本地 Docker 构建 |
| 本地 Windows、Docker Desktop | 不属于 AWS 费用，不计入 |

ECR 仓库包含 manifest index 与其指向的镜像记录，直接把 `describe-images` 所有 `imageSizeInBytes` 相加会近似重复计算同一发布镜像；本表只估算实际两份镜像层的大小，并向上取整。[ECR 定价与扫描说明](https://aws.amazon.com/ecr/pricing/)

## 6. 看日志和未来本地 Agent 可能额外产生什么费用

**普通打开日志页面，不等于启动了 Live Tail。**`Start tailing` 是专门的实时会话，按会话分钟计费：每月前 1,800 分钟免费，之后 $0.01/分钟。若单个会话按 730 小时整月开启，月费约 **(43,800 − 1,800) × $0.01 = $420**。这是使用场景估算，不表示你现在已经开启或产生了这笔费用。[Live Tail 定价](https://aws.amazon.com/cloudwatch/pricing/)

Logs Insights 查询按扫描量计费，加拿大中部 $0.0055/GB。每次只查最近 15–30 分钟通常很小；重复扫描全部历史会增加费用。

未来本地 Agent 的采集方式也值得注意：`GetMetricData` 按请求的指标数收费，为 $0.01/1,000 个指标。本次核对 235 条指标的一轮请求约 $0.00235。但如果以后每分钟把 212 条指标全部查询一次，730 小时约 **$92.86/月**；每 5 分钟查一次仍约 $18.57/月。这不是当前后台费用：本次采集是按需执行，当前 `diagnostic.py` 并没有每分钟查询全部指标。

因此将来应按排错需求选择少量指标、使用合理间隔和时间游标，避免每轮重扫整段历史。查询次数、日志下载流量也要计入；目前按需采集量小，主表未把假设的全天 Agent 或 Live Tail 会话塞进基准月费。[CloudWatch 请求单价目录](https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AmazonCloudWatch/current/ca-central-1/index.json)

## 7. 优化优先级

1. **分步评估 Task 容量。**本次已完成 1 vCPU / 4 GiB 降配，Fargate 算力约 $46.73/月。降配前平均约使用 0.25 vCPU、0.29 GiB 内存；之后可测试 1 vCPU / 2 GiB，算力将进一步降到约 $39.63/月，再省 $7.10。降低内存需要调整容器内存预留，并在真实听说、重连、视频处理时验证峰值；本次没有降低内存。
2. **不要长期闲置 Live Tail，也不要让未来 Agent 高频拉全量指标。**两项都可能超过日志本身的费用。
3. **先保留有诊断价值的观测。**两路应用日志当前写入总计只约 $0.20/月；将健康日志从每 15 秒改成每 60 秒可以减少这些记录，但不会按同样比例降低 $14.84 的指标时间序列费。
4. **确定 EFS 回复音频与转录保留策略。**当前很小，不代表使用几个月之后仍然很小。不要未经决定直接删除家庭数据或备份。
5. **若长期只在部分时间使用，再考虑定时启停。**Task 停止可以节省算力、公网 IP 和部分活跃指标费，但存储、Secret、备份等不会全归零；ECS Service 必须把 desiredCount 改为 0，单独停一个 Task 会被服务补起。

初次成本调查为只读。用户随后要求降低 CPU，本报告已追加 1 vCPU 降配说明；日志频率、保留期、监控开关和计费计划未改动，也没有开通自动诊断服务。

## 8. 可复核材料

本项目 `.local/cost-review/` 保存了原始盘点、三小时 `IncomingBytes/IncomingLogEvents`、网络速率、活跃指标列表、备份计划、官方价格目录以及 `calculation.json`。这些是降配前的本地私密工作材料，未提交 Git；其中 `calculation.json` 保留原始 2 vCPU 基线，本报告月费表已按 1 vCPU 重算。

`cloud/cost_inventory.py`、`cloud/cost_active_metrics.py`、`cloud/cost_prices.py` 为本次只读调查脚本。价格目录在调查时下载，公开目录 publicationDate 为 2026-08-31；实际计费仍以 AWS 当期账单为准。


## 追加说明：1 vCPU 降配与增强监控的必要性

2026-09-10 用户提出降低 CPU 成本。本次仅把 Task CPU 从 2048 单位改为 1024 单位，即 2 → 1 vCPU；两个业务容器的相对 CPU shares 同步从各 1024 改成各 512。它们共享 1 vCPU，Linux CPU shares 是竞争时的相对权重，并不是各自只能使用 0.5 vCPU 的硬上限。内存保持 4 GiB，Enhanced 监控保持开启。

降配前复查 04:01–07:01 UTC 的 180 个一分钟数据点：2 vCPU 的平均 CPU 使用率 12.53%，Maximum 采样最高 16.18%；内存平均 7.22%。当前连接负载支持尝试降配，但尚未验证真实连续对话时的峰值、播放质量与延迟，不能把它当成完整容量测试。

| 方案，730 小时/月 | CPU | 内存 | Fargate 算力合计 | AWS 整体估算 |
|---|---:|---:|---:|---:|
| 原配置：2 vCPU / 4 GiB，Enhanced 开启 | $65.06 | $14.21 | $79.26 | 约 $110–120 |
| 本次降配：1 vCPU / 4 GiB，Enhanced 开启 | $32.53 | $14.21 | $46.73 | 约 $77–88 |
| 可选：1 vCPU / 4 GiB，关闭 Container Insights | $32.53 | $14.21 | $46.73 | 约 $62–73 |

金额为 USD、未税、不含外部 API。最后一行未执行。整体估算沿用相同网络速率与账户共享免费出站额度的两种情况，包含其他已部署 AWS 资源；持续更高对话量、日志、查询和文件增长会改变金额。变更当月按各规格的实际运行时间计费，不会把已经运行的 2 vCPU 时段追溯改成 1 vCPU。

Enhanced 的 212 条，是本次测量时正在上报的独立指标时间序列：指标名称与 Cluster、Service、Task、Container 等维度组合共同决定一条序列。同一个 CPU 信息可能同时提供多个汇总层级，因此不是 212 个容器，也不是按应用日志条数收费。按这个活跃规模持续一个月，212 × $0.07 = $14.84；规模和活跃时长改变时费用也会变化。[CloudWatch 官方计费示例](https://aws.amazon.com/cloudwatch/pricing/)

最初开启的理由，是满足未来诊断 agent 对每个容器 CPU、内存、网络和存储状态的需求，方便关联故障前后的历史数据。它不会自动诊断或修复故障，也不代表 AWS DevOps Agent 已接入。对于只有一个 Task、两个业务容器的实验阶段，它属于可选开销。

关闭 Container Insights 后，现有 awslogs 应用日志仍正常写入 `/ebo-cloud/engine` 与 `/ebo-cloud/assistant`；默认的 AWS/ECS 服务 CPU、内存监控以及 ECS 当前任务健康和停止原因仍可读取。自动采集的容器级性能日志和增强指标停止产生。已有的四条应用 EMF 指标另行计费，不随它自动关闭。默认 ECS 服务指标与付费的普通版 Container Insights 是两回事，不能把 Enhanced 改成普通 enabled 就称为免费。[AWS ECS 监控说明](https://docs.aws.amazon.com/AmazonECS/latest/developerguide/cloudwatch-metrics.html)

建议：先验证 1 vCPU 下实际对话质量；如果当前重点是最低实验成本，可关闭 Container Insights，每月进一步省约 $14.84。若仍需要每个容器的历史指标，之后可以设计少量定向采集，但这需要实现与验证，当前并未部署该替代方案。

降配状态：CloudFormation 变更集 `cpu-1vcpu-20260910` 已核验只改动 TaskDefinition 与 Service，栈已 UPDATE_COMPLETE，TaskDefinition 为 `ebo-cloud-lab:3`（1 vCPU / 4 GiB）。首次新 Task 进程健康，但 Enabot 登录发生 TCP 连接超时；已在相同规格下替换一次。最终 Task `<task-id-redacted>` 两容器 HEALTHY、ECS 部署 COMPLETED，Assistant 运行 132.8 秒、启动宽限期结束后业务健康为 true，音视频输入与 Realtime 连接正常。人工听说体验仍待验证。
